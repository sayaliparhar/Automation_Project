kind: Deployment
apiVersion: apps/v1
metadata:
  name: backend-deployment
  namespace: coding-cloud
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      imagePullSecrets:
      - name: docker-secret
      containers:
      - name: backend
        image: pritam44/coding-cloud-backend:latest
        imagePullPolicy: Always
      ##  resources:
      ##    requests:
      ##      cpu: "256Mi"
      ##      memory: "200m"
        ports:
        - containerPort: 5000
        livenessProbe:
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 5000
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3

---
kind: Service
apiVersion: v1
metadata:
  name: backend-service
  namespace: coding-cloud
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
  - protocol: TCP
    port: 5000
    targetPort: 5000
  sessionAffinity: None
